name: CI

# windMindOM CI — autonomous worker 飛輪的測試閘門（WMOM-20260603-02）。
# 每個 PR（及 merge 進 main 的 push）都自動跑 backend pytest + frontend
# vitest/tsc/build。CI 全綠是 auto-merge.yml 自動合併 bot PR 的前提。
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

# 同一分支新 push 取消還在跑的舊 run，省 runner、加快回饋。
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend pytest（workflow + cost + reporting + knowledge）
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: 安裝依賴（runtime + dev）
        run: pip install -r requirements.txt -r requirements-dev.txt
      - name: pytest（baseline 638 passed / 1 xfailed）
        run: >-
          python -m pytest
          modules/workflow/tests/
          modules/cost/tests/
          modules/reporting/tests/
          modules/knowledge/tests/
          -q

  frontend:
    name: Frontend vitest + tsc + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: 安裝依賴
        run: npm ci
      - name: 型別檢查
        run: npx tsc --noEmit
      - name: 單元測試（baseline 59 passed）
        run: npx vitest run
      - name: production build
        run: npx vite build
