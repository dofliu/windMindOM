# 2026-05-21 WMOM-20260509-F2/F3/F4/F5 — M4 follow-up 小修一次清掉

> **Issues**：WMOM-20260509-F2/F3/F4/F5（4 個 0.5h-1h，code-review follow-up）
> **Branch**：`claude/blissful-turing-4USlF`（autonomous daily worker 2026-05-21 20:00 cron）
> **Goal**：M4 backend cosmetic / perf 4 個 follow-up 一次清掉，把 open issue 從 19 降到 15，淨化 codebase

---

## 1. 為什麼今天做這個

handoff doc（2026-05-19 F1）列下次候選 4 條：
- WMOM-20260519-01（F1 follow-up，0.5-1d；需 design 決策 4 選 1，autonomous session 風險中等）
- **F2-F5 一次清掉**（4 個 0.5h-1h；well-scoped、無 design 模糊；安全 + 高價值）
- M5 規劃（需讀 ROADMAP M5 章節決策，single session 不易完整完工）
- WMOM-20260513-02 demo orchestrator simulator（2-3d，太大）

F2-F5 是 single-session 最佳落地：每個都範圍明確、不涉商業邏輯改動、有具體 file:line 指向。一次處理 4 個小修把 backend follow-up 清光。

---

## 2. 設計決策

### 2.1 F2 — `list_items` / `list` 用 `func.count`

從 `count_stmt = base.with_only_columns(InventoryItemORM.id)` + `len(...scalars().all())` 改為：
```python
count_stmt = select(func.count()).select_from(base.subquery())
total = sess.execute(count_stmt).scalar_one()
```

兩處：
- `inventory_repository.py:273-274`
- `material_request_repository.py:233-234`

**為何 `.select_from(base.subquery())` 而非 `.select_from(InventoryItemORM)`**：base 已加 WHERE 條件（farm_id / warehouse_id / below_safety_only），subquery wrap 直接複用所有 filter；不需重複貼條件。

### 2.2 F3 — `list_warehouses` 加 repo method

router 內 `with repo._sessionmaker() as sess:` 直接撈 ORM 違反 repo 封裝。加 `InventoryRepository.list_warehouses(farm_id: str) -> list[Warehouse]`，router 一行 call。

不破 API contract（response shape 不變）。

### 2.3 F4 — `_FARM_REGISTRY` lazy singleton 抽 shared

4 routers 各自重複：
- `modules/workflow/routers/inventory_router.py:75-92`
- `modules/workflow/routers/material_request_router.py:82-96`
- `modules/workflow/routers/approval_router.py:125-141`
- `modules/cost/routers/cost_ledger_router.py:57-72`

抽到 `shared/farm_registry_provider.py`：
- `resolve_farm_db_path(farm_id: str) -> str`：lazy singleton + import + lookup + raise HTTPException
- `reset_farm_registry()`：給 `set_*_factory(None)` 路徑用（清 singleton 讓下次 lazy 重 init）

各 router 移除 `_FARM_REGISTRY` global + `_resolve_farm_db_path`，改 call shared module。

**為何不 inject** 而是用 module singleton？
- 4 個 router 都用 FastAPI module-level state（非 DI），現狀就是這樣設計
- 全改 DI 是 scope creep（refactor 涵蓋面太大；不在 F4 scope）
- 改用 shared module 維持「同 process 同 singleton」語義，但消滅 4 份重複

**測試 hook**：保留 `reset_farm_registry()` public function 讓測試清 state；現有 `set_*_factory(None)` 流程改 call shared `reset_farm_registry()`。

### 2.4 F5 — `InventoryAdjustmentLog.actor_id` 改 Optional

支援系統自動 adjustment（如未來 dispatch hook 直接 adjust，無人工 actor）：
- domain `InventoryAdjustmentLog.actor_id: UUID` → `UUID | None = None`
- ORM `InventoryAdjustmentLogORM.actor_id`：`Mapped[str]` → `Mapped[Optional[str]]`（nullable=True）
- repo `adjust()` 簽名：`actor_id: UUID | None = None`
- repo `_log_to_domain`：`UUID(orm.actor_id) if orm.actor_id else None`
- schema `AdjustInventoryRequest.actor_id`：→ `Optional[UUID] = None`
- schema `AdjustmentLogResponse.actor_id`：→ `Optional[UUID] = None`

**ORM column 是否需要 migration？**
- 本專案 SQLite + `Base.metadata.create_all`，無正規 migration framework
- 新表沒問題；舊表 NULL 寫不進 NOT NULL 欄 → 但目前是 single-dev 階段，dev DB 隨時可重建
- 真正生產時走 alembic 加 migration（出 M4 release 時統一處理；本 issue scope 不含）

### 2.5 不修改

- F1 / F6（其他 follow-up）
- domain 層 `MaterialRequest`（不動）
- frontend（actor_id Optional 影響 frontend：但前端 `AdjustmentForm` 一律帶 actor_id，不影響；schema accept None 是後端兼容性，前端維持必填）
- 月報 / cost ledger 邏輯（不動）

---

## 3. 檔案異動（規劃）

### 3.1 修改 backend

**F2**：
```
M modules/workflow/repository/inventory_repository.py       list_items: count → func.count() + subquery
M modules/workflow/repository/material_request_repository.py list: count → func.count() + subquery
```

**F3**：
```
M modules/workflow/repository/inventory_repository.py  加 list_warehouses(farm_id) method
M modules/workflow/routers/inventory_router.py         list_warehouses: 改用 repo method
```

**F4**：
```
+ shared/farm_registry_provider.py        新 module（lazy singleton + resolve + reset）
M modules/workflow/routers/inventory_router.py         改用 shared
M modules/workflow/routers/material_request_router.py  改用 shared
M modules/workflow/routers/approval_router.py          改用 shared
M modules/cost/routers/cost_ledger_router.py           改用 shared
```

**F5**：
```
M modules/workflow/domain/inventory.py                 actor_id Optional
M modules/workflow/repository/inventory_orm.py         InventoryAdjustmentLogORM.actor_id nullable
M modules/workflow/repository/inventory_repository.py  adjust signature + _log_to_domain Optional
M modules/workflow/schemas/inventory_schemas.py        AdjustInventoryRequest + AdjustmentLogResponse Optional
```

### 3.2 修改 tests

```
+ modules/workflow/tests/test_f2_count_uses_sql_func.py   F2 perf regression（撈 count 不 fetch rows）
+ modules/workflow/tests/test_f3_list_warehouses_repo.py  F3 unit test for repo method
+ modules/workflow/tests/test_f4_shared_farm_registry.py  F4 shared module unit test
+ modules/workflow/tests/test_f5_adjust_actor_optional.py F5 actor_id None 接受 + log Optional 回 null
```

### 3.3 修改 tracking

```
M ISSUES.md         F2/F3/F4/F5 → done；issue_stats open 19→15 / done 37→41
M STATUS.yaml       last_updated / next_milestone
+ work-logs/2026-05/2026-05-21-f2-f5-cleanup.md  本檔
```

---

## 4. TODO

- [x] Read existing code（F2/F3/F4/F5 4 處）
- [x] Branch + work-log
- [x] F2: list_items / list 改 func.count + subquery
- [x] F3: list_warehouses 加 repo method（含 router 切換）
- [x] F4: shared/farm_registry_provider.py + 4 routers 全切換 + cost_ledger HTTPException unused import 清掉
- [x] F5: actor_id Optional（domain / ORM nullable / repo signature / schema / frontend type + UI fallback）
- [x] Add 16 → 18 unit tests（F2 ×3 含 event.listen / F3 ×3 / F4 ×4 / F5 ×8 含 migration ×2）
- [x] backend pytest 全跑 zero regression（545+1 baseline → 569+1 with 18 new；3 pre-existing numpy drift 不變）
- [x] frontend tsc clean + vite build 5.46s 917.19 kB（+0.04 kB vs baseline）
- [x] code-reviewer subagent（3 must-fix + 5 should-fix + 3 nice + 1 domain；採納 8/11；見 §5.4）
- [x] ISSUES.md / STATUS.yaml update
- [x] commit + push + PR（review fix 走 follow-up commit）

---

## 5. 實作紀錄

### 5.1 完成檔案

**Backend 修改（7 個檔）：**
- `modules/workflow/repository/inventory_repository.py` — 加 `from sqlalchemy import func` + F2 list_items count 改 SQL-side + F3 `list_warehouses(farm_id)` 新 method + F5 `adjust(..., actor_id=None)` 簽名 + str 轉換 None-aware + `_log_to_domain` None-aware
- `modules/workflow/repository/material_request_repository.py` — F2 list count 改 SQL-side（`func` 已 import）
- `modules/workflow/repository/inventory_orm.py` — F5 `InventoryAdjustmentLogORM.actor_id` → `Mapped[Optional[str]]` + `nullable=True`
- `modules/workflow/domain/inventory.py` — F5 `InventoryAdjustmentLog.actor_id` Optional default None（field 順序調整：把 `id` 移到 `actor_id` 前，維持 dataclass「required first, default after」layout）
- `modules/workflow/schemas/inventory_schemas.py` — F5 `AdjustInventoryRequest.actor_id` + `AdjustmentLogResponse.actor_id` 都 Optional
- `modules/workflow/routers/inventory_router.py` — F3 router 改 call `repo.list_warehouses`（移除 lazy import + raw ORM SQL）+ F4 切 shared registry
- `modules/workflow/routers/material_request_router.py` — F4 切 shared registry
- `modules/workflow/routers/approval_router.py` — F4 切 shared registry
- `modules/cost/routers/cost_ledger_router.py` — F4 切 shared registry + 清 unused `HTTPException` import

**Backend 新檔（2 個）：**
- `shared/farm_registry_provider.py` — F4 共用 `resolve_farm_db_path(farm_id) -> str` + `reset_farm_registry()`（含 docstring 說明為何不用 DI / 為何採 module-level singleton）
- `modules/workflow/tests/test_f2_f5_followups.py` — 16 個 test 覆蓋 F2-F5 4 個 follow-up

**Frontend 修改（2 個檔）：**
- `frontend/services/inventoryService.ts` — F5 `AdjustmentLogResponse.actor_id: string | null`
- `frontend/components/workflow/InventoryDetailDrawer.tsx` — F5 顯示 fallback：`actor_id ? …{last8} : ui('system', '系統')`

### 5.2 設計決策落實

- **F2 subquery 包 base**：選 `select(func.count()).select_from(base.subquery())` 而非 `select(func.count()).select_from(InventoryItemORM)` — base 已含所有 WHERE filter（farm_id / warehouse_id / below_safety_only / status），subquery wrap 直接複用；不需重複貼條件
- **F3 ordering 與既有 router 行為一致**：`is_default DESC, name ASC`（default 倉排首位給 UI 預選用）
- **F4 不改 DI**：4 router 都採 module-level state，全改 DI 涵蓋面太大；shared module 維持「同 process 同 singleton」語義消滅 4 份重複；`reset_farm_registry()` public 給 `set_*_factory(None)` 流程清 state 用
- **F5 field 順序**：`id` 移到 `actor_id` 前面，避開 dataclass「non-default before default」rule；所有現有 call site 都用 kwargs（test 3 處皆 kwargs），不破 positional 兼容
- **F5 ORM nullable 不寫 migration**：本專案 SQLite + `Base.metadata.create_all`，dev DB 隨時可重建；生產走 alembic 時統一處理（不在 F5 scope）
- **F5 frontend 顯示 fallback**：既有 `InventoryDetailDrawer.tsx:472` 直接 `log.actor_id.slice(-8)` 若拿到 null 會 runtime crash；改為 `actor_id ? …{last8} : ui('system', '系統')` 與 i18n 一致

### 5.3 Build / test 驗證

- **Baseline**：`python -m pytest modules/{workflow,cost,reporting}/tests/ -q` → **545 passed + 1 xfailed + 3 pre-existing numpy drift**（test_monte_carlo_k13_seed_42 / test_mc_percentiles_pinned / test_varfluct_year_1_pinned — 與 5/19 handoff 一致）
- **本次 session 跑完整 test 套件**：`561 passed (+16 new) + 1 xfailed + 3 pre-existing numpy drift` — **zero regression**
- **E2E**：`python -m pytest tests/e2e/ -q` → 6 passed
- **Frontend**：`npx tsc --noEmit` → exit=0；`npx vite build` → 5.46s, 748 modules transformed, 917.19 kB（gzip 264.96 kB）— vs baseline 917.15 kB +0.04 kB（F5 UI 加 1 行 fallback 顯示）

### 5.4 Code review 採納

Code-reviewer subagent 跑出 **3 must-fix + 5 should-fix + 3 nice-to-have + 1 domain concern**，
**採納 8/11**（3 must-fix 全採；5 should-fix 採 4；3 nice-to-have 採 1 + 1 留 follow-up）：

| 級別 | # | 議題 | 修法 |
|------|---|------|------|
| Must-fix | 1 | `shared/` 不允許 import `modules/` 架構違反 | 把 `farm_registry_provider.py` 從 `shared/` 搬到 `modules/monitoring/server/`（與 underlying `FarmRegistry` class 同 package） |
| Must-fix | 2 | F5 既有 DB `actor_id NOT NULL` 對 `actor_id=None` 寫入會 `IntegrityError` | 加 `_migrate_actor_id_nullable_sqlite(engine)` idempotent migration：PRAGMA 檢查 + SQLite batch rebuild（CREATE 新表 + INSERT SELECT + DROP + RENAME + 重建 index）；在 `get_inventory_repository` 第一次 open DB 時跑 |
| Must-fix | 3 | `reset_farm_registry()` 跨 router 全域副作用未文件化 | 4 個 router `set_*_factory()` docstring + provider module docstring 都加「⚠ 全域操作 / 影響其他 3 個 router」警示 |
| Should-fix | 1 | `db_path is None` 404 guard 對真實 `FarmRegistry` 是死碼 | 改用 `_farm_registry.get_farm(farm_id) is None` 判斷；test fixture FakeRegistry 同步加 `get_farm` method |
| Should-fix | 2 | F2 test 只驗 SQL pattern 等價，不擋日後改回 `len()` | 改用 `event.listen("before_cursor_execute")` 攔 production 真實 SQL，斷言至少一句 `count(` |
| Should-fix | 3 | F5 dataclass field 順序改變（actor_id 移到 id 後）破閱讀順序 | 還原 actor_id 排在 reason 後（與原 schema 一致），改 default=None 即可（兩個有預設值的欄位順序自由） |
| Should-fix | 4 | work-log §5/§6 待填 | 補完（即本節） |
| Should-fix | 5 | 4 router docstring 對 reset 副作用措辭不一 | 同 Must-fix #3 統一加「⚠ 全域操作」警示 |
| Nice | 1 | F2 subquery 比 `count(id) WHERE ...` 多搬欄位 | 不採（百萬量級才有差，本場景 < 1000 rows） |
| Nice | 2 | F5 `actor_id` Field 缺 OpenAPI description | 加 `Field(default=None, description="...")` |
| Nice | 3 | F3 `WHERE farm_id` filter 是冗餘（單 farm 一個 DB） | 不採（pre-existing 設計，pattern 一致性 > 微優化） |
| Domain | 1 | `actor_id=None` 無 `source_type` 區分系統來源 | 留 follow-up（未來 dispatch hook 落地時補 `source: Literal["manual", "system"]`） |

Follow-up 留作未來：domain concern source_type 欄位（待 dispatch hook 真實需求出現時補 issue）。
Migration tests 2 個新加（`test_f5_migration_converts_old_notnull_db_to_nullable` +
`test_f5_migration_idempotent_on_already_nullable`），fixture 用 raw `sqlite3` 建舊
schema 驗證 PRAGMA + batch rebuild 行為。

---

## 6. 下次接手指南

### 已完成（本 session）
- M4 backend follow-up F2/F3/F4/F5 全 done — code-review tech debt 從 5 條（F1-F5）砍到 1 條（剩 F6 留 M6）
- 新 `shared/farm_registry_provider.py` 將來 farm DB path 邏輯異動單點維護
- 16 個新 test 對 F2/F3/F4/F5 各自 invariant 保護

### 待辦（不阻塞）
- WMOM-20260519-01 — `add_return` 超量退料 domain guard 評估（F1 follow-up，0.5-1d；需 design 決策 4 選 1）
- WMOM-20260509-F6 — PostgreSQL row-lock integration test（M6 部署前處理）
- WMOM-20260513-02 — Demo Orchestrator simulator integration（2-3d，M5 demo polish）
- M5 規劃 — 讀 `docs/product/ROADMAP.md` M5 章節決定第一個 issue（RAG knowledge module skeleton 或 demo orchestrator UI 完整版）

### 建議下次 session 工作
1. **M5 規劃 + 第一個 issue 切出**：本月 M4 主線 + follow-up 全收，下一步是 M5 knowledge / RAG。讀 ROADMAP M5 + decision_log，切第一個 issue（建議從 RAG skeleton 開始，warming up）
2. **WMOM-20260519-01**：F1 follow-up domain guard，做完月報極端場景 demo 不再有負值風險
3. **WMOM-20260513-02**：demo orchestrator simulator integration（A10 follow-up）

