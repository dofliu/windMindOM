name: windMindOM
name_zh: 風心智運維平台
category: wind_energy
progress: 100
status: active
last_updated: "2026-09-26"  # **2026-09-26 第七個 autonomous session（WMOM-20260926-04
  # 完成）** — `WorkOrderDetailModal.tsx`（legacy mock 版，掛在 `App.tsx` 'maintenance'
  # 導覽路徑，由 `MaintenanceHub.onSelectWorkOrder` 觸發，與 backend `WorkOrderResponse`
  # 版本的 `components/workflow/WorkOrderDetailModal.tsx` 是兩支不同元件）先前零
  # component render 測試，新增 `components/__tests__/WorkOrderDetailModal.test.tsx`
  # （17 tests），**未修改元件本體任何一行**。涵蓋標題/詳情/技師名稱 fallback、狀態
  # pill 三態、關閉互動（遮罩/Close 鈕/內容區不冒泡）、備註編輯+Save、照片上傳（jsdom
  # 無原生 `DataTransfer`，手刻 `makeFileList()` stub 繞過）/移除、Complete 流程（無
  # 照片 disabled 阻擋/有照片送出正確更新後物件）、COMPLETED 唯讀模式。5 項關鍵邏輯
  # mutation-verified。**誠實揭露**：`handleComplete` 內部邏輯層 guard（`if (photos.
  # length>0)`）無法獨立於 DOM 層 `disabled` 屬性被測到（jsdom 對 disabled button 不
  # 派發 click handler），是防禦性重複、非本次新增缺口，已誠實標註。backend 1274
  # passed 不變；frontend tsc 0、1443→**1460 passed**（68→69 files，+17，零
  # regression）、build OK。**附帶 housekeeping**：`WMOM-20260505-21` Status 過期
  # 未同步（其 follow-up `WMOM-20260926-01` 早已全數完成）已更正為 done；ISSUES.md
  # 頂部「🎯 未來大目標」摘要表 M5-5 行過期敘述已更正。**前一 session（第六個
  # autonomous session，WMOM-20260926-03
  # 完成）** — PR C Phase 1 實作：情境掛載唯讀端點 + FarmOverview/TurbineDetail 接線。
  # 後端新增 `Storage.scenario_turbine_ids()` + `GET /api/scenarios/{id}/turbines`/
  # `farm-status`（格式對齊即時 `/api/turbines*`，重用 `get_history(limit=1,...)` 查詢
  # 先例）；前端新增 `ScenarioMountContext`（內建 `useScenarioMountData` 單次
  # fetch——情境資料是凍結快照不 polling/WS，`turbines`/`loading`/`error` 隨 context
  # 分發）+ `ScenarioMountBanner` + `ScenarioDetail`/`ScenarioPage`/`App.tsx` 掛載入口
  # 串接（`utils/turbineNaming.ts::wtIdToTurbineIndex` 換算 WT-id → `TurbineData.id`）+
  # `FarmOverview`/`TurbineDetail` 停用所有即時資料/寫入子面板——header
  # 限載/停機/安排檢查三鈕、右欄 `OperatorControlCard`（整張替換成停用說明卡，非僅
  # disable 按鈕）、主圖 `TrendChartPanel`、farm-trend 趨勢圖、匯出鈕、AI 診斷卡派遣鈕。
  # **設計判斷**：情境內機組 id（WT001, WT002...）與即時 simulator 機組 id 共用同一套
  # `WT{i:03d}` 命名，若不停用這些子面板，它們會用同編號打即時端點——使用者以為在唯讀
  # 檢視凍結情境，實際上看到/能操作的是剛好同編號的真實即時風機，比「忘記 disable 寫入
  # 按鈕」更隱蔽（GET 輪詢正常回應、畫面看起來一切正常）。**code-reviewer subagent
  # review（獨立 async agent，46 次工具呼叫）抓到 1 must-fix + 3 should-fix + 2
  # nice-to-have，全數已修復並 mutation-verified**：①🔴 must-fix——情境掛載後永遠無法
  # 回報 FAULT 狀態：`simulator.engine._tur_state_to_str` 映射表結構上只有
  # IDLE/STARTING/RUNNING/STOPPING 四種、無 FAULT 分支，即時路徑的 FAULT 覆寫靠的是
  # `data_broker._sim_output_to_reading` 對記憶體內 `FaultEngine` 當下狀態的第二段覆寫，
  # 情境是批次生成，那份記憶體狀態生成完就消失，DB 也沒有為每筆 reading 存 tripped
  # 旗標——一個刻意注入故障的情境掛載後會整面顯示「健康」，直接牴觸同一情境自己「趨勢」
  # 頁籤看到的故障標記，是核心用途的正確性回歸。本 session 獨立重讀
  # `_tur_state_to_str`/`_sim_output_to_reading` 兩段式邏輯確認 reviewer 論述無誤，修法：
  # 新增 `_tripped_fault_turbine_ids()` 純函式，依已持久化的故障注入事件
  # （`history_events`，`event_type=="fault"`，含 payload 的
  # `severityRate`/`initialSeverity`）用與 `FaultEngine.step()` **完全相同的公式**
  # （`severity=min(1,initial+rate*elapsed)`，`tripped=severity>=
  # FAULT_SCENARIOS[id].auto_trip_severity`）離線重算，不需新 schema、重用既有已記錄
  # 資料；同一機組同一 scenario_id 的重複注入比照 `FaultEngine.inject()` 的 dedup
  # 語意只取最後一次。新增 8 測（2 整合測試用真 `generate_bulk` 注入高/低嚴重度故障、
  # 1 無排程回歸、5 純函式測試涵蓋 dedup/時序/未知情境/缺欄位），mutation-verified（拔掉
  # 覆寫邏輯 → 整合測試如預期 fail；dedup 改成取第一次 → dedup 測試如預期 fail）。②🟡
  # `turState=int(row.get("tur_state") or 6)` 用 `or` 會把 storage 真實會存的 `0`
  # （scada 缺 tag 時的預設值）誤判成缺值、silently 捏造成 6（正常發電），改用
  # `is not None` 判斷，補 2 regression test mutation-verified。③🟡
  # `handleMountScenario` 的「瀏覽總覽」分支未清 `pendingMountTurbineDataId`——若使用者
  # 先點「瀏覽機組細節」又在資料到位前改點「瀏覽總覽」，補選 effect 仍會強制導去
  # TurbineDetail 蓋掉使用者最後選擇，已在該分支補清空（App.tsx 無 render 測試基礎設施，
  # 僅程式碼閱讀驗證）。④🟡 `useScenarioMountData` 的 `error` 算出來後從未浮現在畫面
  # 上——情境被刪除（404）或網路失敗時 banner 仍自信顯示「情境檢視中」卻是空資料，順便
  # 把 fetch 從 App.tsx 移進 `ScenarioMountProvider` 內建（單一 fetch 來源），
  # `turbines`/`loading`/`error` 隨 context 分發，`FarmOverview`/`TurbineDetail`
  # 直接讀取在 banner 顯示錯誤說明，新增 9 個 context 測試 + 2 個 banner 錯誤顯示測試皆
  # mutation-verified。⑤🟢 `handleOpenDispatchModal` 補獨立 `scenarioMount.mounted`
  # guard（派遣目前唯一入口已 disabled，但不依賴呼叫端自律）。⑥🟢 異常 turbine_id 格式
  # 補 `logger.warning`（原本靜默 fallback idx=1 會與真正 WT001 撞名）。**誠實揭露**：
  # App.tsx 本身無 render 測試基礎設施（既有限制，需 mock ~15 個子元件）；前端刻意不
  # 消費新的 `/farm-status` 端點（`FarmOverview` 本就從 `turbines` prop 自行加總，該
  # 端點仍完整測試於後端供未來使用）；FAULT 重算繼承 `history_events` 既有的「無
  # session_id、靠時間窗撈取」限制（`DEC-20260719-01`），非本次新引入風險。backend
  # 1255→**1274 passed**（+19，7 skipped, 1 xfailed，零 regression）；frontend tsc
  # 0、1402→**1443 passed**（65→68 files，+41，零 regression）、build OK。
  # `WMOM-20260926-03` 標 done——`DEC-20260720-01`/`DEC-20260720-02` 的 PR C 至此
  # 完整收尾（Phase 2 工單/維護 what-if 演練維持明確 deferred，未評估）。**前一 session
  # （2026-09-26 第五個 autonomous session，WMOM-20260926-02 完成）** — PR C（情境掛載
  # app）子設計定案：`WMOM-20260926-01`
  # 完成後唯一剩下的「可立即接手」項目 PR C（DEC-20260720-01 §3、DEC-20260720-02 A2
  # epic 最後剩餘項目）自 2026-07-20 立案以來被至少 8 個 autonomous session 因「動核心
  # 來源模型、需獨立子設計」原地擱置未動工。本 session 先用 Explore agent 徹底讀過
  # `data_broker.py`（全 1053 行）、`routers/source.py`（全 94 行）、`routers/
  # turbines.py`、`app.py` WS broadcast、`routers/scenarios.py`（全 438 行）、
  # decision_log.md DEC-20260720-01/-02 全文、`ScenarioPage.tsx`/`App.tsx` 導覽，
  # 確認三個關鍵事實：①`DataBroker` 是單一 active source 模型（`get_all_turbines`/
  # `get_turbine` 硬性要求 `mode==SIMULATION and self.simulator`，`select_view_only`
  # 刻意讓兩者回空）；②`FarmOverview`/`TurbineDetail`/`MaintenanceHub` 全部直接吃
  # broker live REST/WS，無既有「資料來源可替換」抽象層；③DEC-20260720-01 原案（broker
  # 新增情境檢視來源狀態）會在 `WMOM-20260720-04`/`-08` 剛用 4×30 併發壓測硬化好的
  # `_lifecycle_lock`/單一 active source 並發關鍵區上疊加新變動面，風險與既有投資衝突。
  # 拒絕原案，改採**與 broker 完全正交**的方案：`view` mode 語意不變（不升級）；新增 2
  # 個唯讀端點 `GET /api/scenarios/{id}/turbines`/`farm-status`（重用 `routers/
  # scenarios.py` 已有的 `get_history(limit=1,session_id=...)`/`scenario_turbine_
  # aggregates` 查詢先例，純附加不動 `data_broker.py`/`source.py` 一行）；前端新增獨立
  # `ScenarioMountContext`（非新 `/api/source/select` mode），與是否有 live/simulation
  # 在跑正交（不互斥）。範圍明確切成 **Phase 1**（`FarmOverview`+`TurbineDetail` 唯讀掛載
  # + banner + 寫入操作全 disabled，本次唯一批准範圍）/ **Phase 2**（deferred 不評估：
  # decision log 原文「工單/維護在這份資料上演練」需要 workflow 模組加 `scenario_id`
  # 隔離 + 全新語意決策，是獨立更大設計題）——這個切分正是本 issue 反覆被 punt 8 次的
  # 根因修正：過去把「唯讀掛載」與「可操作 what-if 演練」混在同一個「PR C」標籤下評估，
  # 導致每次都覺得太大而跳過。決策寫入 `docs/product/decision_log.md` `DEC-20260926-01`
  # （含拒絕原案的具體理由、若劉老師認為掛載該與 live 互斥時的備選方案）。開新 follow-up
  # **`WMOM-20260926-03`**（Phase 1 實作，open，deliverable 已完整寫明含 API 形狀/檔案
  # 清單/範圍邊界，下個 session 無需再重新調查即可直接接手）。**本 session 純設計/文件，
  # 零程式碼變更**：backend 1255 passed（7 skipped, 1 xfailed）/ frontend 1402 passed
  # （65 files）/ tsc 0 / build OK 皆與變更前一致（無程式碼改動故不需 mutation-verify）。
  # `WMOM-20260926-02` 標 done。**前一 session（2026-09-26 第四個 autonomous session
  # ，WMOM-20260926-01 三項全數完成，issue 標 done）**：`work_order.finish()` →
  # day_work_form 自動寫入 hook——工單完工（`approve_all`：`AWAITING_SIGNOFF →
  # CLOSED`）時，自動幫該工單 `assignee_id` 對應員工當天日誌 append 一筆
  # `completed_wo` activity。掛在 `WorkOrderRepository.transition()` 內部
  # `action == "approve_all"`——唯一能同時覆蓋 `approval_router` 簽核鏈自動觸發與
  # `work_order_router` 直接 `/approve` 入口兩條完工路徑的地方（兩者皆直接呼叫
  # `repo.transition()`，不經過 router 層 `_run_transition`）。`work_date` 用
  # `ZoneInfo("Asia/Taipei")` 曆日，比照前端 `todayAsiaTaipei()` 慣例；day_work_form
  # 寫入失敗（如 DB lock）整段包 `try/except Exception` 只 log 不 raise，不影響已
  # `sess.commit()` 落地的工單關閉本身——比照 `approval_router.approve_step` 既有
  # catch+log 模式。**code-reviewer subagent review：Approve，0 must-fix，2
  # should-fix + 2 nice-to-have，2 個 should-fix 皆已修復**：①`DayWorkFormORM.
  # __table__.create(checkfirst=True)` 補建 schema 的呼叫其實已多餘——reviewer 獨立
  # 追出 `modules/workflow/repository/__init__.py` 一律先 import
  # `.work_order_repository` 才 import `.day_work_form_orm`/
  # `.day_work_form_repository`，代表任何拿得到 `WorkOrderRepository` 的呼叫路徑，
  # `DayWorkFormORM` 早已註冊在共用 `Base.metadata` 上，`get_repository()` 的
  # `create_all()` 一定會連帶建好該表；多打的這次 checkfirst 呼叫反而在工單完工這條
  # 熱路徑上每次都多開一條 engine 連線 + 觸發 `BEGIN IMMEDIATE` 寫鎖 round trip，
  # 造成不必要的延遲/鎖競爭——已刪除該呼叫，本 session 獨立重讀 `__init__.py` 的
  # import 順序確認 reviewer 論述無誤才動手改，改註解說明本模組頂層 import
  # `DayWorkFormORM`（純為註冊副作用）已足夠保證 table 存在，不依賴 `__init__.py`
  # 順序。②`test_approve_all_via_work_order_router_approve_endpoint_also_appends`
  # 名稱宣稱測 `work_order_router` 直接入口，實際內容跟另一個測試一樣直接呼叫
  # `repo.transition()`，從未碰觸任何 router，是誤導性的假覆蓋——已刪除，改在
  # `test_approval_api.py` 新增
  # `test_approve_last_step_closes_work_order_also_appends_day_work_form_activity`：
  # 完整跑過 FastAPI `TestClient` + 真正的簽核鏈兩階簽核 HTTP 流程，驗證 production
  # 唯一真實觸發路徑（`approval_router` 簽核鏈最後一階）也確實觸發 day_work_form
  # 自動寫入。③🟢 nice-to-have（已採納）：hook 建立日誌時補
  # `created_by=wo.assignee_id`（比照 `day_work_form_router` 慣例，純 audit trail
  # 一致性，reviewer 確認不影響 ownership 授權——授權比對的是 `employee_id` 不是
  # `created_by`）。④🟢 nice-to-have（記錄，未修）：`except Exception` 範圍較寬，
  # 理論上也會吞掉程式本身的 bug 而非僅預期的 DB 暫時性失敗，但與既有
  # `approval_router.py` 慣例一致，維持不改。reviewer 另外獨立驗證（非發現問題）：
  # circular import 只能靠 lazy import 避開（有實際追出 module-level import 會在
  # `_ENGINE_LOCK` 定義之前觸發 partial-init `ImportError` 的反例）；hook 放在
  # `with self._sessionmaker()` block 外面才呼叫，正確避免跟工單自己的
  # `BEGIN IMMEDIATE` 寫鎖疊在一起自我鎖死；None-assignee 防禦性檢查在 state
  # machine 圖上理論不可達但正確放在 try block 外（不會被吞掉）；Asia/Taipei 只用於
  # natural key 日期分桶，`ActivityEntry.logged_at` 仍是 UTC，語意正確；多 WO /
  # offshore 生命週期（含 reopen 再走一輪完工累加多筆 activity）無正確性問題。新增/
  # 調整測試皆 mutation-verified（含 review 修復後第三輪，逐一改回舊邏輯確認新測會
  # fail 再還原）。backend 1250→**1255 passed**（7 skipped, 1 xfailed，+5，零
  # regression）；frontend 未動 1402 passed 不變、tsc 0、build OK。
  # `WMOM-20260926-01` 三項全數完成，**issue 狀態改標 done**。
  #
  # **前一 session（2026-09-26 第三個 autonomous session，WMOM-20260926-01
  # 第 3 項完成）**：`day_work_form` 讀取端點 ownership 限制——`list`/`by-date`/`{form_id}`
  # 3 個讀取端點先前只檢查「有沒有登入」，未限制查詢範圍（含 TREASURY 也能瀏覽任一員工
  # 任一天完整日誌，issue 原文點名的問題案例）。新增 `modules/auth/dependencies.py::
  # resolve_actor_when_enforced`（enforce=false 過渡期不限制、完全不解 token，比照
  # `require_role`/`require_authenticated` 的 enforce-aware gate 風格，與既有
  # `resolve_actor_id` 系列「無論 enforce 與否一律優先信任 token」的 identity-resolution
  # 語意刻意區隔）；`_FULL_VISIBILITY_ROLES = {LEADER, SUPERVISOR, ADMIN}` 維持可查全員，
  # EMPLOYEE 與 TREASURY 收窄成只能查自己（list 靜默覆寫 employee_id filter、by-date
  # 必填參數非本人 403、detail 404-before-403 比照既有 `append_activity` 慣例）。
  # **code-reviewer subagent review：Approve，0 must-fix，2 should-fix + 2 nice-to-have，
  # 皆已處理**：①新測試原本只用 TREASURY 驗證「非本人」路徑，EMPLOYEE（本次修法主要
  # 呼叫者）與別人交集的案例留白——補 3 個 EMPLOYEE 版本測試（list/by-date/detail）；
  # ②work-log mutation 驗證/測試數字兩節原留 TODO——已回填；③`_viewer_uuid` 與既有
  # `_employee_uuid` 重複的 try/UUID/400 pattern——已採納，抽出共用 `_to_uuid(label,
  # value)` helper；④cutover 前建議確認前端無依賴 TREASURY 舊行為畫面——記錄供 M6
  # 部署前 checklist（本次不動前端）。reviewer 獨立追蹤完整授權邏輯鏈路 + repository
  # 層查詢語意，確認無 bypass（enforce=true 下無任何靜默失敗開放路徑、list 覆寫對
  # 「帶入」與「不帶」employee_id 皆無差別覆寫、三端點的覆寫/403/404-403 行為差異是
  # 刻意設計非安全缺口）。新增 13 個測試皆 mutation-verified（`git stash` 暫存實作檔
  # 重跑確認核心測試如預期 fail，含針對 review 補測的第二輪用檔案覆寫方式驗證）。
  # backend 1237→**1250 passed**（+13，零 regression）；frontend 未動 1402 passed
  # 不變、tsc 0、build OK。`WMOM-20260926-01` 狀態改標 `in_progress`（第 1、3 項完成，
  # 第 2 項 `work_order.finish()` hook 仍 open——已讀完 `state_machine.py` +
  # `approval_router.py`，掛點答案已寫入 work-log：`WorkOrderRepository.transition()`
  # 內部 `action == "approve_all"` 時觸發，是唯一能同時覆蓋 `approval_router` 自動
  # 觸發與 `work_order_router` 直接 `/approve` endpoint 兩條路徑的地方）。**前一
  # session（2026-09-26 第二個 autonomous session，WMOM-20260926-01 第 1 項完成）**：
  # `day_work_form` 前端（工作日誌 tab，本人專用）——
  # `services/dayWorkFormService.ts`（API client，`readError` 比照 `knowledgeService.ts`
  # 解析陣列 422 detail）+ `hooks/useDayWorkForm.ts`（`workDate` driver 查當日日誌 +
  # 最近歷史僅本人；`appendActivity` 一律先 `getOrCreate`）+
  # `components/workflow/DayWorkFormPanel.tsx`（日期選擇 + 活動列表 + 新增活動表單 4
  # kind + 歷史）+ `statusUtils.ts`（`activityKindLabel`/`todayAsiaTaipei`）+
  # `WorkflowPage.tsx`（新增 daywork tab）。**code-reviewer subagent review 抓到 1
  # must-fix + 3 should-fix + 3 nice-to-have，must-fix 與 3 個 should-fix 全數已
  # 修復**：①`useDayWorkForm.appendActivity` 的 `setForm(updated)` 缺對稱 race
  # 防護（`fetchForm` 已有 `abortRef` 但 append 完成時沒有同款保護）——使用者送出
  # 活動期間切換日期，較慢的 append 回應後到會蓋回舊日期資料且不自我修正，補
  # `latestWorkDateRef` 比對送出當下捕捉的 `workDate` 是否仍是最新值；reviewer 判定
  # `useInspectionSchedules`「無專屬 hook test」慣例不適用本 hook（有跨請求
  # staleness + 日期綁定可變目標），已補 `hooks/__tests__/useDayWorkForm.test.ts`
  # （10 測，含精準重現此 race 的回歸測試）；②`myWorkOrderOptions` 誤共用 Orders
  # tab 已被 `statusFilter`/`search` 過濾的 `wo.items`——改掛獨立
  # `useWorkOrders({ farmId })` instance；③`item_id` 缺 UUID 格式驗證 + 錯誤訊息
  # 對現場工程師不友善——補 regex 前置驗證 + `readError` 改用 `knowledgeService.ts`
  # 既有的陣列 detail 解析手法；④過期 `woId` 未重新對齊——補 `stillValid` 比對，
  # **mutation test 過程中額外發現並修正這個修法本身漏處理「清單變空」邊界、以及
  # 第一版回歸測試斷言 `select.value`（被瀏覽器原生 DOM fallback 掩蓋）選錯觀察
  # channel 的問題，改斷言送出 payload 才真正鎖住**。2 個 nice-to-have 已採納（已
  # 記錄過工單自動排除選單 + `fmtDate` docstring 補充安全性假設）；1 個記錄未修
  # （送出中按鈕 `aria-busy` 需動共用 `Btn` primitive，blast radius 較大）。reviewer
  # 獨立核對「本人限定」scope-narrowing 前後端一致無漏洞。frontend
  # 1354→**1402 passed**（63→65 files，+48，零 regression）；backend 未動 1237
  # passed 不變、tsc 0、build OK。`WMOM-20260926-01` 狀態改標 `in_progress`（第 1
  # 項完成，第 2 項 `work_order.finish()` hook + 第 3 項讀取端點 ownership 限制仍
  # open，皆需設計決策）。**前一 session（2026-09-26 第一個 autonomous
  # session，WMOM-20260505-21 後端完成）**：
  # `day_work_form` 員工當天工作日誌後端——domain（`ActivityKind` enum + `ActivityEntry`/
  # `DayWorkForm` dataclass + `validate_activity_entry` 純函式 + `ACTIVITY_REQUIRED_FIELDS`
  # 表）+ repository（`DayWorkFormRepository`：`(farm_id, employee_id, work_date)` 唯一索引
  # natural-key `get_or_create_for_date` + 累加式 `append_activity` + query）+ router（5
  # endpoints：get-or-create/list/by-date/detail/append-activity）全數完成，55 個新測試皆
  # mutation-verified。**code-reviewer subagent review 抓到 1 must-fix + 3 should-fix + 2
  # nice-to-have，must-fix 與前兩項 should-fix 已修復**：①`append_activity` 完全沒有
  # ownership 檢查，任何在職員工可竄改別人的日誌，且**我自己寫的測試字面上把這個錯誤行為
  # 鎖成預期通過**（`test_append_activity_enforced_employee_ok` 建立者與 append 呼叫者用
  # 兩個不同隨機 subject 卻斷言 200）——reviewer 用既有測試斷言反證出 bug，修法：
  # `append_day_work_form_activity` 先 404 查無日誌，再用 `_employee_uuid` 解出呼叫者身分
  # 比對 `form.employee_id`，不同者一律 403（不分角色，LEADER/SUPERVISOR 也不能代填）；
  # ②domain 與 schema 各自維護一份 `_REQUIRED_FIELDS` 逐字複製、無 parity 測試——已改成
  # domain 匯出公開 `ACTIVITY_REQUIRED_FIELDS`，schema 直接 import 同一物件（`is` 比對確認
  # 單一真實來源）；③`_select_by_natural_key` 缺 `Session` 型別標註已補。讀取端點（list/
  # by-date/detail）無 ownership 限制（reviewer 判定非阻塞本 PR）+ work_order.finish() 整合
  # hook + 前端頁面，拆成新 follow-up **WMOM-20260926-01**（open）。新增 3 個 regression test
  # 鎖住 ownership 修復（過渡期換人 403 + enforce 換人 403 + SUPERVISOR 代填仍 403），皆
  # mutation-verified（拔掉 ownership 比對 → 3 測如預期 fail）。backend 1182→**1237
  # passed**（7 skipped, 1 xfailed，+55，零 regression）；frontend 未動 1354 passed（63
  # files）不變、tsc 0、build OK。`WMOM-20260505-21` 狀態標 `in_progress`（後端 done，前端
  # 待補），非直接 `done`。**前一 session（2026-09-25 autonomous session，WMOM-20260925-05，
  # 完成）**：
  # `WMOM-20260505-22` 前端收尾——`inspection_schedule` 定檢計畫管理併入既有
  # `WorkflowPage.tsx`（新增第 5 個 tab「定檢計畫」，非獨立路由）+ `TurbineDetail.tsx`
  # header『安排檢查』鈕接上 `onNavigateInspection(turbine.name)` 深連結（App.tsx 新增
  # `inspectionDeepLinkTurbineId` state，`handleNavSelect` 一般導覽時清空避免深連結過濾
  # 殘留跨 session）。新增 `inspectionScheduleService.ts` + `useInspectionSchedules` hook
  # （沿用 `useInventory` 模式）+ `InspectionScheduleListPanel`/
  # `CreateInspectionScheduleModal`/`InspectionScheduleDetailModal` 三支元件。新增 66 測
  # 皆 mutation-verified。**code-reviewer subagent review 抓到 2 must-fix + 2
  # should-fix，皆已修復**：①深連結 `onNavigateInspection` 呼叫順序寫反，同一 event
  # handler 內兩個 `setState` 被 React batch 導致深連結恆為 no-op——改成
  # `handleNavSelect(id, opts)` 單一 setState 呼叫的結構性修法；②`activate`/
  # `deactivate` 誤用 `patchLocal` 違反自己 docstring 宣稱的 refetch 行為，
  # `active_only` filter 下暫停計畫不會即時消失——已改為 `fetchList()`；③
  # `CreateInspectionScheduleModal` 的 `canSubmit` 未驗證 turbineId 是否仍在
  # turbineOptions 內，已改用 `.some(...)` 並補 regression test。backend 未動；
  # frontend tsc 0、`npx vitest run` 63 files **1354 passed**（零 regression）、
  # build OK。`WMOM-20260505-22`（後端+前端）與 `WMOM-20260507-02`（PageHeader
  # placeholder 按鈕清單，6 個 sub-task 全數完成）皆標 done。誠實揭露：`App.tsx`
  # 深連結 state 管理無自動化測試保護（`App.tsx` 本身無 test 檔，既有慣例——這正是
  # must-fix #1 沒被自動化測試抓到、只能靠 code review 人工發現的根本原因）；
  # `useInspectionSchedules` hook 內部邏輯無獨立單元測試（沿用同款 workflow CRUD
  # hook 既有模式）。**前一 session（WMOM-20260505-22 後端，完成；前端拆
  # WMOM-20260925-05）**：`inspection_schedule` 定檢計畫 + scheduler auto-spawn 後端全套——
  # domain（`Recurrence` enum + `compute_next_due`/`recurrence_interval_days` 純函式 +
  # `InspectionSchedule` dataclass）+ repository（`InspectionScheduleRepository`：CRUD +
  # `due_only` 查詢 + `record_spawn`，共用 `work_order_repository` engine cache）+ service
  # （`run_inspection_scheduler`：手動觸發、冪等、撞 multi-WO constraint 跳過不推進不漏排；
  # 刻意不加背景 cron，比照 reporting/cost 既有 on-demand 慣例，避免重蹈 WMOM-20260720-04/-08
  # 背景執行緒硬化債）+ router（7 endpoints，CRUD=LEADER/SUPERVISOR、list/get=任何登入者、
  # `run-scheduler`=SUPERVISOR only）+ 79 個新測試（domain/repository/service/router 四層）
  # 皆 mutation-verified。**code-reviewer subagent review 抓到 1 must-fix + 1
  # should-fix，皆已修復**：PATCH 可把排程改成 `recurrence=custom_days` 卻不帶
  # `interval_days` 寫入無效狀態，scheduler 下次到期時 `work_order_repo.create()` 先成功建
  # 真實工單、`record_spawn()` 才 raise `ValueError`（未 catch）——工單已建但 `next_due_at`
  # 沒推進，變成每次呼叫 `run-scheduler` 都重複 spawn（reviewer 獨立寫 repro script 實測
  # 重現「兩次呼叫→兩張重複工單+兩次 500」）；`create()` 同款驗證原本只在沒帶
  # `first_due_at` 時才會跑，帶了會繞過。修法：三層防禦（`create()` 無條件驗證 +
  # `update_metadata()` 驗證合併後最終狀態 + scheduler 建工單前再驗證一次），新增 6 個
  # regression test 逐一 mutation-verified（其中 scheduler 層 mutation 精確重現 reviewer
  # repro script 的同一個 traceback，證實修法確實堵住那個洞，不是修了另一個問題）。
  # backend 1103→**1182 passed**（7 skipped, 1 xfailed，+79，零 regression）；frontend
  # 未動 1288 passed（60 files）不變、tsc 0、build OK。**前端未做**（`TurbineDetail.tsx`
  # 安排檢查鈕接線 + 定檢計畫管理頁），拆成新 follow-up **WMOM-20260925-05**（open，API
  # 已齊全可直接接手）。`WMOM-20260505-22` 狀態標 `in_progress`（非 `done`，避免誤判整個
  # issue 已完工）。**前一 session（WMOM-20260925-04，完成）**：`FarmOverview.tsx` PageHeader
  # 『+ 新報告』鈕接線（`WMOM-20260507-02` sub-task f）——認領前依 issue 建議先讀
  # `modules/reporting/routers/reporting_router.py` 摸清報告類型（僅 monthly/annual-budget
  # 兩種），發現 `ReportsPage.tsx`（`/admin/reports`，M4 `WMOM-20260509-09` 早已完整實作
  # monthly/annual 兩 tab + HTML preview + PDF 下載）已涵蓋 issue 原文「開 modal 選報告
  # 類型」所需功能，**刻意偏離原文寫法**改把按鈕接到既有頁面導覽（`FarmOverviewProps` 新增
  # 必填 `onNavigateReports: () => void`，`App.tsx` 傳入 `() => handleNavSelect('reports')`，
  # 與 sidebar nav 同路徑），避免重造功能較弱的重複子集 modal，已於 issue/work-log 明確記錄
  # 避免未來 session 誤判重複開工。新增 2 測（zh/en aria-label 點擊接線）皆
  # mutation-verified。backend 未動 1103 passed 不變；frontend tsc 0、1286→1288 passed
  # （60 files，零 regression）、build OK。code-reviewer subagent review：**Approve，0
  # must-fix，0 should-fix**（獨立核對必填 prop 慣例一致、`handleNavSelect('reports')` 無
  # 副作用、測試非同義反覆、`ReportsPage` 目的地乾淨；提醒溝通風險已記錄化解）。
  # **`WMOM-20260507-02` 清單至此僅剩 sub-task d**（依賴 `WMOM-20260505-22`
  # `inspection_schedule` 尚未做，持續卡著）。**前一 session（WMOM-20260925-03，完成）**：`MaintenanceHub.tsx` PageHeader『+ 新工單』鈕接線（`WMOM-20260507-02` sub-task e）——新增 `NewWorkOrderModal`（比照 `TurbineDetail.tsx` `CurtailModal`/`DispatchModal`/`FarmSelector.tsx` `CreateFarmModal` 遮罩/`role="dialog"` 慣例）：風機 `Select`（新增 `turbines` prop，非寫死清單）+ 問題描述 `textarea` + 技師 `Select`（僅列 `ON_DUTY`，比照 `DispatchModal` 既有「只能派遣在崗者」規則）。送出打既有 `maintenanceData.createWorkOrder`（`useMaintenanceData.ts` 早已實作、原供 `DispatchModal` 使用，`POST /api/maintenance/work-orders`，`SUPERVISOR`-only），成功後關窗（不額外維護送出狀態，比照 sub-task b/c 決策）。`createWorkOrder` 第 4 參數 `technicianId` 型別由必填改選填（對齊後端 `Optional[int]` 既有契約）。`App.tsx` 呼叫點補 `turbines`/`lang`（`lang` 為附帶發現的既有缺口，先前完全未傳，一併修正）。新增 11 測（開關 dialog、必填欄位擋送出、`technicianId` undefined/number 兩路徑、技師下拉僅列 `ON_DUTY`、風機選項來自 prop、lang=en、`turbines=[]` 邊界、**已選技師於 10s 輪詢期間變成非 `ON_DUTY` → 送出視同未指派**），皆 mutation-verified。backend 未動 1103 passed 不變；frontend tsc 0、1275→1286 passed（60 files，+11 新測，零 regression）、build OK。code-reviewer subagent review：**Approve，0 must-fix**，2 should-fix（①技師選取與輪詢資料脫節可能送出過期 id，已修＋補測 mutation-verified；②選擇沿用 `DispatchModal` fire-and-forget 模式而非 `CurtailModal` 完整 error state 寫法，reviewer 確認屬已誠實揭露的既有系統性缺口不阻塞，未修）+ 3 nice-to-have（`aria-label` 缺失為新舊 modal 慣例並存的既有現象未採納；`turbines=[]` 邊界測試已採納；死碼 `useMockMaintenanceData.ts` 型別未同步放寬不影響未採納）。`WMOM-20260507-02` 清單至此僅剩 sub-task d（依賴 `WMOM-20260505-22`）與 f（依賴確認 reporting module 報告類型）。**前一 session（WMOM-20260925-02，完成）**：`TurbineDetail.tsx` PageHeader『限載』鈕接線（`WMOM-20260507-02` sub-task c）——新增 `CurtailModal`（比照 `FarmSelector.tsx` 的 `CreateFarmModal` 遮罩/`role="dialog"` 慣例）收 kW 值後打 `authFetch POST /api/control/curtail`（`SUPERVISOR`-only），是右欄 `OperatorControlCard` 限載輸入的重複入口（比照 sub-task b『停機』header 鈕先例，不重寫 `OperatorControlCard` 邏輯）。留空 = 解除限載（語意同既有 `setCurtail`）、前端擋負值/非數字、後端非 2xx 顯示 `detail` 訊息不關窗。新增 8 測（開關 dialog、送出 kW 值/留空解除、前端擋負值、後端錯誤顯示、`authFetch` header）皆 mutation-verified（本 session 1 輪 + code-reviewer 獨立額外 2 輪：移除負值檢查、body 漏 `turbineId`，皆如預期 fail）；另修共用 `stubFetch()` helper 補 `ok: true`（原缺此欄位會讓新寫的 `res.ok` 檢查誤判成功回應為失敗）。backend 未動 1103 passed 不變；frontend tsc 0 error、1267→1275 passed（60 files 不變，+8 新測，零 regression）、build OK。code-reviewer subagent review：**Approve，0 must-fix**，1 should-fix（issue 狀態同步收尾，已修）、1 nice-to-have（`CurtailModal` 送出中 Cancel/✕ 未 disable——逐字複製 `CreateFarmModal` 既有相同結構，非本次新增問題，未採納）。`WMOM-20260507-02` 清單尚餘 sub-task d~f（`d` 依賴 `WMOM-20260505-22` `inspection_schedule` 尚未做；`e`/`f` 無阻塞可續接）。**前一 session（WMOM-20260925-01，完成）**：`frontend/hooks/*.ts` authFetch 稽核缺口一次做完全部 sub-task（未依 issue 建議拆多 session，因 4 支 hook 修法完全一致、無設計歧義）：`useSettings.ts`（`POST /api/config/simulation`+`/datasource`，皆 `require_role(SUPERVISOR)`）、`useMaintenanceData.ts`（2 GET `require_authenticated()` + 3 個 `SUPERVISOR` 寫入 + 2 內部 refresh GET）、`useRealtimeData.ts`（初始 REST fetch + WS 斷線 5 秒輪詢 fallback，皆 `GET /api/turbines`）、`useI18n.ts`（`GET /api/i18n/tags/all`），以及 review 過程中 `WMOM-20260924-08` 已標記待查、本次一併確認的 `App.tsx:166`（`GET /api/farms` 後端健康檢查）——共 13 處裸 `fetch(` 改 `authFetch(`，逐一重讀後端 router 源碼（`config.py`/`maintenance.py`/`turbines.py`/`i18n.py`/`farms.py`）核對角色設定與 issue 描述完全一致。新增/追加測試 11 個（`useSettings` +2、`useMaintenanceData` 新建測試檔 +4、`useRealtimeData` +3、`useI18n` 新建測試檔 +2），皆逐一 mutation-verified（改回裸 fetch → 對應「已登入」header 斷言如預期 fail，用 scratchpad 備份而非 `git checkout` 還原確認）。`frontend/` 全樹遞迴 grep 確認無裸 fetch 殘留（除 `authClient.ts` 自身 wrapper 實作 + 刻意繞過 401 handler 的 `authApi.login`）。`App.tsx` 該處因元件 525 行、零 render test 基礎設施（需 mock ~15 個子元件）判斷超出本次範圍，僅程式碼閱讀 + `tsc --noEmit` 驗證，已誠實記錄於 work-log（無自動化保護）。已回頭把 `docs/product/WMOM-20260716-05_auth_enforcement_plan.md` §6 cutover 檢查表「前端所有寫入 request 都帶 token」**重新勾選**——**M6 auth cutover 的前端側阻塞至此清除**（cutover 本身〔翻 `WMOM_AUTH_ENFORCE=true`〕仍是獨立、需另評估時機的動作，非本次範圍）。backend 未動 1103 passed 不變；frontend tsc 0 error、1256→1267 passed（58→60 files，+11 新測，零 regression）、build OK。code-reviewer subagent review：**Approve，0 must-fix**，2 nice-to-have（①`useSettings.ts` 兩個寫入呼叫是 fire-and-forget、401 錯誤只 console.warn 未浮現 UI，屬既有行為非本次引入，宜另開「UI 浮現 auth 錯誤」issue，未採納；②reviewer 獨立確認 `frontend/` 全樹〔非僅 `hooks/`〕已無裸 fetch 殘留，記錄性非動作項）。**前一 session（WMOM-20260924-08）**：`components/field/MyOrdersMode.tsx` 的 `fetchActiveFarmId()`（GET `/api/farms`，供「我的工單」列表過濾用，後端 `require_authenticated()`）僅存一處裸 `fetch` 改 `authFetch`，比照姊妹 PR 系列手法。新增 2 測（已登入時 mount GET 帶 `Authorization` header；未登入時驗證過渡期行為不變），皆 mutation-verified（authFetch 改回裸 fetch → 已登入測試如預期 fail `expected undefined to be 'Bearer test-token-myorders'`，用 scratchpad 備份而非 `git checkout` 還原確認）。backend 未動 1103 passed 不變；frontend 1254→1256 passed（+2 新測，58 files 不變）、tsc 0、build OK。code-reviewer subagent review：Approve，0 must-fix，1 should-fix（追蹤檔案時序，已補齊）、2 nice-to-have（測試檔 hook 疊加未採納；獨立驗證 hooks 層缺口與本 session 發現一致）。**⚠ 重大發現**：本 session 主動遞迴重掃 `frontend/hooks/*.ts`（`WMOM-20260923-10` 系列稽核從未涵蓋此目錄），發現 `hooks/useSettings.ts`（`POST /api/config/simulation` + `POST /api/config/datasource`，皆 `require_role(SUPERVISOR)`）與 `hooks/useMaintenanceData.ts`（`PATCH .../technicians/{id}/status`、`POST`/`PATCH /work-orders`，皆 `require_role(SUPERVISOR)`）內的裸 fetch **寫入呼叫**仍缺 auth header——嚴重度高於已修復的純讀取缺口，`WMOM_AUTH_ENFORCE=true` cutover 後會讓 `MaintenanceHub`/`SettingsPage` 的寫入操作整面靜默 401。code-reviewer subagent 用獨立 recursive grep 得到相同結論（另補一處 `App.tsx:166` 待下個 session 核對）。已開新 issue **WMOM-20260925-01**（high priority）追蹤，並回頭把 `docs/product/WMOM-20260716-05_auth_enforcement_plan.md` §6 cutover 檢查表「前端所有寫入 request 都帶 token」**取消勾選**（該勾選現已證實不成立，完成 WMOM-20260925-01 前不得翻 `WMOM_AUTH_ENFORCE=true`）。**前一 session（WMOM-20260923-08）**：`components/field/MyOrdersMode.tsx` 的 `fetchActiveFarmId()`（GET `/api/farms`，供「我的工單」列表過濾用，後端 `require_authenticated()`）僅存一處裸 `fetch` 改 `authFetch`，比照姊妹 PR 系列手法。新增 2 測（已登入時 mount GET 帶 `Authorization` header；未登入時驗證過渡期行為不變），皆 mutation-verified（authFetch 改回裸 fetch → 已登入測試如預期 fail `expected undefined to be 'Bearer test-token-myorders'`，用 scratchpad 備份而非 `git checkout` 還原確認）。backend 未動 1103 passed 不變；frontend 1254→1256 passed（+2 新測，58 files 不變）、tsc 0、build OK。code-reviewer subagent review：Approve，0 must-fix，1 should-fix（追蹤檔案時序，已補齊）、2 nice-to-have（測試檔 hook 疊加未採納；獨立驗證 hooks 層缺口與本 session 發現一致）。**⚠ 重大發現**：本 session 主動遞迴重掃 `frontend/hooks/*.ts`（`WMOM-20260923-10` 系列稽核從未涵蓋此目錄），發現 `hooks/useSettings.ts`（`POST /api/config/simulation` + `POST /api/config/datasource`，皆 `require_role(SUPERVISOR)`）與 `hooks/useMaintenanceData.ts`（`PATCH .../technicians/{id}/status`、`POST`/`PATCH /work-orders`，皆 `require_role(SUPERVISOR)`）內的裸 fetch **寫入呼叫**仍缺 auth header——嚴重度高於已修復的純讀取缺口，`WMOM_AUTH_ENFORCE=true` cutover 後會讓 `MaintenanceHub`/`SettingsPage` 的寫入操作整面靜默 401。code-reviewer subagent 用獨立 recursive grep 得到相同結論（另補一處 `App.tsx:166` 待下個 session 核對）。已開新 issue **WMOM-20260925-01**（high priority）追蹤，並回頭把 `docs/product/WMOM-20260716-05_auth_enforcement_plan.md` §6 cutover 檢查表「前端所有寫入 request 都帶 token」**取消勾選**（該勾選現已證實不成立，完成 WMOM-20260925-01 前不得翻 `WMOM_AUTH_ENFORCE=true`）。**前一 session（WMOM-20260923-08）**：`FarmOverview.tsx` 的 `TrendCard` 內僅存一處裸 `fetch`（`/api/turbines/farm-trend`，後端 `require_authenticated()` 任何登入者可讀）改 `authFetch`，比照同檔案 WMOM-20260923-07 匯出鈕已建立的手法。新增 2 測（已登入時 mount GET 帶 `Authorization` header；未登入時驗證過渡期行為不變），皆 mutation-verified（authFetch 改回裸 fetch → 已登入測試如預期 fail `Cannot read properties of undefined (reading 'headers')`，用 scratchpad 備份而非 `git checkout` 還原確認）。backend 未動 1103 passed 不變；frontend 1252→1254 passed（+2 新測，58 files 不變）、tsc 0、build OK。此為 `WMOM-20260923-07` review 時登記的 nice-to-have follow-up，`FarmOverview.tsx` 全檔至此無裸 `fetch` 殘留。code-reviewer subagent review：Approve，0 must-fix，1 should-fix（範圍外發現：`components/field/MyOrdersMode.tsx:31` 仍是裸 fetch——`WMOM-20260923-10` 原稽核指令未遞迴子目錄漏掉這支現場工程師頁面，登記為新 follow-up **WMOM-20260924-08**、非本次範圍）。**前一 session（WMOM-20260924-07）**：`WMOM-20260923-10` 前端 authFetch 稽核清單 sub-task 7/7（清單收尾）——`TrendChartPanel.tsx`（即時趨勢圖面板）2 處裸 fetch（mount 時 GET `/api/i18n/tags` + mount/preset/自訂 tag/turbineId 變更時且每 2 秒輪詢 GET `/api/turbines/{id}/trend`，皆任何登入者可讀）改 `authFetch`，比照姊妹 PR WMOM-20260923-07/-09/WMOM-20260924-01~06 手法，新增 2 測（已登入時兩條 mount GET 皆帶 header + 未登入行為不變對照組）皆 mutation-verified（authFetch 改回裸 fetch → 已登入測試如預期 fail，用備份而非 git checkout 還原確認）。backend 未動 1103 passed 不變；frontend 1250→1252 passed、tsc 0、build OK。**`WMOM-20260923-10` 稽核清單（7 支元件）至此全數完成，已標 done**，並回頭勾掉 `docs/product/WMOM-20260716-05_auth_enforcement_plan.md` §6 cutover 檢查表「前端所有寫入 request 都帶 token」項目。code-reviewer review：Approve，0 must-fix、0 should-fix（確認 2 秒輪詢下 `authFetch` 401 handler 重複觸發為既有跨元件已接受特性，非本次新增問題）。**前一 session（WMOM-20260924-06）**：`HistoryPage.tsx` 2 處裸 fetch 改 `authFetch`，新增 2 測皆 mutation-verified，frontend 1248→1250 passed。**前一 session（WMOM-20260924-05）**：`EventComparisonView.tsx` 1 處裸 fetch（`/api/maintenance/events/compare`）改 `authFetch`，新增 2 測皆 mutation-verified，frontend 1246→1248 passed。**前一 session（WMOM-20260924-04）**：`WMOM-20260923-10` 前端 authFetch 稽核清單 sub-task 4/7——`CostPage.tsx`（`/admin/cost` 成本模型頁）1 處裸 fetch（mount 時 GET `/api/farms`，供 dataset/farm selector，`require_authenticated()` 任何登入者可讀）改 `authFetch`，比照姊妹 PR WMOM-20260923-07/-09/-20260924-01/-02/-03 手法，新增 2 測（已登入 mount GET 帶 header + 未登入行為不變對照組）皆 mutation-verified（authFetch 改回裸 fetch → 已登入測試如預期 fail，用備份而非 git checkout 還原確認）。backend 未動 1103 passed 不變；frontend 1244→1246 passed、tsc 0、build OK。code-reviewer review：Approve，0 must-fix（附帶說明：reviewer 過程中誤執行 `git checkout` 重置 working tree，已自行發現並手動重建，本 session 事後獨立以 `git diff` + 全套重跑確認內容與行為未受影響）。清單尚餘 3 支純讀取元件（`EventComparisonView`/`HistoryPage`/`TrendChartPanel`）。**前一 session（WMOM-20260924-03）**：`SettingsPage.tsx` 11 處裸 fetch（5 GET + 6 SUPERVISOR-only 寫入）全改 `authFetch`，新增 8 測皆 mutation-verified，0 must-fix Approve，frontend 1236→1244 passed。**前一 session（WMOM-20260924-02）**：`WMOM-20260923-10` 前端 authFetch 稽核清單 sub-task 2/7——`FarmSelector.tsx`（sidebar 底部風場切換器 + 建立風場 modal）3 處裸 fetch（GET 列表 + POST 切換/建立，後兩者皆 SUPERVISOR-only 寫入）全改 `authFetch`，比照姊妹 PR WMOM-20260923-07/-09/WMOM-20260924-01 手法，新增 4 測（mount GET + 切換 POST + 建立 POST header 斷言 + 未登入行為不變對照組）皆 mutation-verified。code-reviewer review 0 must-fix/should-fix，2 nice-to-have（記錄性，不需改動），Approve。backend 未動 1103 passed 不變；frontend 1232→1236 passed、tsc 0、build OK。清單尚餘 5 支（`SettingsPage` 含 SUPERVISOR 寫入優先）。**前一 session（WMOM-20260924-01）**：`FaultInjectionPanel.tsx` 6 處裸 fetch（含 3 個 SUPERVISOR-only 寫入：注入/清除故障/執行測試計畫）全改 `authFetch`，新增 5 測皆 mutation-verified，0 must-fix，Approve，frontend 1227→1232 passed。情境模式深化 arc：DB-lock 根治（WMOM-20260720-01, #142）+ 情境=凍結資料集 DEC-20260720-01（設定 gate #146 / 產生情境不自由跑 #147）+ 情境比較分析 epic DEC-20260720-02（A0 摘要端點 #148 merged / A1 同情境內比較 #150 merged，round-2 的 4 個 Should-fix follow-up 記在 WMOM-20260720-13）。**2026-09-01**：對外素材 — `promo/` 3 分鐘專案介紹影片落地（WMOM-20260901-01，18 景 HTML 動畫 + storyboard + 180.0s 1080p30 MP4，戲劇景＝Simulator-first；供 M6 客戶接觸 / partner 對接用，見 promo/README.md）。**2026-09-22 autonomous session**：**WMOM-20260720-04 + WMOM-20260720-08 live/OPC 後端硬化收尾**——M6 現場部署唯一硬阻塞的 5 個延後子問題（stop() 未停 OPC thread / 切走 live 無角色檢查 / set_simulation 靜默 switch_mode / start-stop-switch_mode 無鎖 / engine 不可中斷睡眠）一次修完，逐項 mutation-verified（含 4×30 併發壓測腳本直接重現「無鎖時 20s 內 15 次例外」vs「加鎖 0.77s 零例外」）。code-reviewer subagent review 抓到 2 Must-fix（`switch_farm`/`select_view_only` 亦需納入同一把鎖；`OPCDAAdapter._poll_loop` 同款不可中斷睡眠）+ 1 Should-fix（壓測 mock 失效）皆已修復並重新 mutation-verified。monitoring +17 測、backend 1093 passed / frontend 957 passed 全綠回歸。**2026-09-22 autonomous session #2（WMOM-20260922-01）**：補上 session #1 work-log §7 open questions 留的殘留問題——`_loop` accelerated 分支（time_scale>1）的 `time.sleep(wall_sleep)` 同款不可中斷，改 `Event.wait()`，mutation-verified（+1 測，1094 passed）。code-reviewer review：0 Must-fix，1 Should-fix（欄位註解過時）已採納。**session #3（WMOM-20260720-13）**：A1 round-2 review 的 4 個 Should-fix 全修（PR #157 merged）。**session #4（WMOM-20260922-02，PR D）**：`GuidedTourPage.tsx` 5 個純展示子元件（`Eyebrow`/`ModChip`/`Beat`/`Story`/`Row`）原定義在元件函式體內，任何 re-render（含跟情境步驟無關的主題切換）都逼 React 整棵子樹 unmount/remount，提升到 module scope 解決（同款修法已用於同日 `ScenarioTrendView`）；新增 DOM node identity 回歸測試並 mutation-verified；frontend 960→961 passed，backend 未動。code-reviewer review：0 Must-fix/Should-fix，5 Nice-to-have（皆選擇性優化，Approve）。**⚠ CI runner 基礎設施自本日起持續失效**（每一個 CI run 皆在數秒內 `runner_id: 0` 失敗、auto-merge 全數 `skipped`，帳號/組織層級問題，非本 repo 程式碼可修復範圍；PR #157/#158 已由劉老師人工合併，PR #159（追蹤檔案數字更正）待合併），需人工排查 GitHub Actions 帳號設定。**session #5（WMOM-20260922-03）**：情境比較分析 A2 Part 1——新增 `GET /api/scenarios/compare?ids=1,2,3` 跨情境摘要並排端點，重用 A0 單情境摘要邏輯（抽 `_load_scenario_summary` helper 供兩端點共用），純函式 `_parse_compare_ids` 處理去重/排序/數量邊界（2-5）；新增 HTTP 層路由順序守門測試守住「`/compare` 必須註冊在 `/{scenario_id}` 之前」這個易踩雷點（純直呼 endpoint 函式的既有測試對此類 bug 是盲區）。code-reviewer review：0 must-fix，1 should-fix（`compare_scenarios` 改 `asyncio.gather` 平行取代序列 await，呼應設計意圖的效能考量）+ 2 nice-to-have（補 HTTP 層 400 測試、常數搬到消費者附近）已採納，皆 mutation-verified。backend 1094→1103 passed，frontend 未動 961 passed 不變。範圍刻意只做後端摘要並排（比照 A0→A1 節奏），相對時間對齊時序疊圖留給下個 session（A2 Part 2）。**session #6（追蹤檔更正，無程式碼變更）**：PR #162 上板後觀察到 CI 兩個 job 皆拿到真實 runner_id、逐步驟正常跑完、雙雙綠燈，`auto-merge.yml` 全自動 squash-merge 進 main——是當日第一個未秒退的 CI run，CI runner 基礎設施疑似恢復（仍待連續多個 session 觀察確認非偶發，見 TODO.md 新警語）。**session #7（WMOM-20260922-04）**：情境比較分析 A2 Part 2 前端——消費前一 session 已完成但無前端呼叫方的 `GET /api/scenarios/compare`，新增 `ScenarioCompareAcrossView.tsx`（風場層 rollup 跨情境並排：headline 卡片 + 指標選擇 + 長條圖 + 全指標並排表，配色複用既有 5 色 chart token）+ `ScenarioPage.tsx` 勾選（2-5 個）/「比較所選」UI，與 A1 對稱（比較單位是情境而非機組）。開發過程中自行抓到並修正 2 個真實 bug（並排表誤用目前選中指標的格式化函式格式化每一列；刪除已勾選情境未同步清除勾選，會讓後續 `/compare` 因殘留失效 id 整批 404），皆測試鎖住 + mutation-verified。code-reviewer review：0 must-fix / 0 should-fix / 3 nice-to-have（皆非阻塞，未採納），Approve。backend 未動 1103 passed 不變；frontend 961→978 passed（+17 新測）、tsc 0、build OK。CI runner 基礎設施查最近 5 個 run 全數 success，連續綠燈樣本持續累積中。**2026-09-23（WMOM-20260923-01）**：`MaintenanceHub` component render 測試——439 行的 `/admin/maintenance` 頁面元件先前零 component 測試（TODO.md 從 2026-06 起多筆 session 點名但一直沒人接手），比照 `DispatchModal` 純 props-driven 範式補上 49 測（PageHeader/Filter/WorkOrderTable 全欄位含 techMap fallback 邊界/RosterCard/WeekCalendar 計數徽章），5 個關鍵邏輯分支手動 mutation-verified。code-reviewer review：0 must-fix，3 should-fix 全數採納（皆為 reviewer 親自 mutation testing 抓到的具體覆蓋率缺口：「最後一位無分隔線」測試斷言與名稱不符只驗證內容存在、`WeekCalendar` 計數徽章只驗證「某處出現」未鎖住星期分桶邏輯、技師目前非 ON_DUTY 時查表 fallback 未覆蓋），production 加一行 `data-testid="week-day-{i}"` 供分桶測試精確 scope，3 個 should-fix 逐一 mutation-verified；另採納 4 nice-to-have，Approve。backend 未動 1103 passed 不變；frontend 978→1027 passed（+49 新測）、tsc 0、build OK。**2026-09-23（WMOM-20260923-02）**：`FaultInjectionPanel` component render 測試——555 行的 `/admin` 故障模擬頁面元件先前零 component 測試，`MaintenanceHub`/`FaultInjectionPanel` 同批 untested 大元件最後一支。元件比 `MaintenanceHub` 複雜（3 條 mount fetch effect + 3s 輪詢 + inject/clear/run 三個 action + message toast 自動清除），比照 `TrendChartPanel`/`SettingsPage` 的 fetch mock + `vi.useFakeTimers()` 範式補上 40 測。6 個關鍵邏輯分支手動 mutation-verified（inject 按鈕 disabled 條件/風機選項數固定 14/執行中互斥 disable/DB 大小 fallback/訊息 3s 清除/3s 輪詢間隔）。code-reviewer review：0 must-fix，2 should-fix 全數採納（`scenario_id ?? name_en` fallback 測試原本無法區分優先順序，補上兩者皆有值且不同的對照測試並 mutation-verified；`.parentElement` DOM 遍歷 scoping 技術債 reviewer 標註優先度低不必卡此 PR，本次不動）+ 3 nice-to-have 未採納（次要覆蓋率缺口），Approve。純測試新增，零 production 變更；backend 未動 1103 passed 不變；frontend 1027→1067 passed（+40 新測）、tsc 0、build OK。**2026-09-23（WMOM-20260923-03）**：情境
比較分析 A2 Part 3——跨情境相對時間對齊時序疊圖。連續兩個 session 把此項標成「需要新後端端點」而
延後，本次重新檢視資料層判定不需要：既有單情境端點 `GET /api/scenarios/{id}/turbines/{tid}/
history`（A1 已在用）+ 前端已持有的 `SavedScenario.config.sim_start` 就足以純前端算相對時間對齊，
零後端變更（決策翻案已記錄於 `docs/product/decision_log.md` DEC-20260923-01，`DEC-20260720-02`
原文加註 superseded）。新增 `utils/scenarioTimeline.ts`（純函式）+
`ScenarioCompareTimelineView.tsx`（新頁籤：機組+指標選擇器、每情境各自 fetch 既有 history 端點、
recharts 各 `<Line>` 自帶 `data` 疊圖、純 DOM 圖例因應 jsdom 無 ResizeObserver 的既有測試限制、
fallback 對齊/截斷/載入失敗三種 banner），`ScenarioCompareAcrossView.tsx` 加頁籤列（摘要並排/
時序疊圖，向後相容 optional prop）。只做疊圖，差異圖（Part 4，需先解決多序列取樣點不完全對齊的
插值/分桶問題）留待下個 session。4 個關鍵分支手動 mutation-verified。code-reviewer subagent
review：**1 must-fix**（架構翻案未寫進 decision_log，已補 DEC-20260923-01）+ **2 should-fix 全數
採納**（recharts 跨線 tooltip 精確比對非最近點、取樣間隔不同時可能只命中部分情境——非 bug，已加
註解+UI 說明；單一情境 fetch 失敗原本靜默併入無資料，新增獨立 `failed` 旗標+banner 區分，2 測+
mutation-verified）+ 1 個 nice-to-have（docstring 歸屬）已修，Approve。backend 未動 1103 passed
不變；frontend 1067→1100 passed（+33 新測）、tsc 0、build OK。**2026-09-23（WMOM-20260923-04）**：
`components/ui` primitives 測試補齊——9 支 UI primitive 檔案（1220 行）先前零 `__tests__`，逐檔
評估後判定 `StatusPill.tsx`（3 支狀態顏色映射純函式）+ `Charts.tsx`（`MiniSparkline`/`BigChart`/
`HealthBar` SVG path 數學 + clamp + 顏色門檻）值得補測試，其餘 7 支（`Btn`/`Card`/`Field`/`Stat`/
`PageHeader`/`Logo`/`Sidebar`）判定 ROI 低或範圍另計，暫不動。新增 27+21=48 測，6 個關鍵分支
mutation-verified。零 production 變更。code-reviewer review：0 must-fix，2 should-fix 全數採納
（issue 補登記進 ISSUES.md/STATUS.yaml；docstring 用詞澄清為本 repo 首次引入此測試模式，非沿用
既有慣例）+ 1 nice-to-have 採納（`BigChart` events 測試改精確 `cx` 座標斷言）+ 1 nice-to-have
不採納（`hexToRgb` helper 重複，僅 2 處不急著抽），Approve。backend 未動 1103 passed 不變；
frontend 1100→1148 passed（53→55 files）、tsc 0、build OK。**2026-09-23（WMOM-20260923-05）**：
`Sidebar.tsx` component render 測試——前一 session 逐檔評估 `components/ui/*.tsx` 時點名
`Sidebar.tsx`（220px 主導覽，294 行，全站唯一主導覽入口）範圍較大另開一支，本次接手：先前完全
零 `__tests__`，新增 23 測（primary/secondary 導覽項目渲染/active 樣式/badge 顯示條件含
`badge=0` falsy 邊界、onSelect+onMobileClose 點擊行為含 desktop 下未傳 `onMobileClose` 的
optional chaining 安全性、backend 健康狀態 dot、lang/theme 切換按鈕、footerExtra 條件渲染、
mobile drawer 響應式行為）。5 個關鍵分支手動 mutation-verified。零 production 變更。
code-reviewer subagent review：**1 must-fix（已修）**——work-log 原稿缺
Implement/Verify/Review/Wrap-up 段落且結尾自我指向不存在內容，已補齊完整 8-phase 記錄；
**2 should-fix 全數採納**：(1) 主題切換測試會透過真實 `ThemeProvider.toggle()` 寫入真實
`localStorage['wmom.theme']`，vitest per-file isolation 下同檔案後續測試可能讀到污染值
（本檔是本 repo 第一支真正呼叫 `toggle()` 的測試），`afterEach` 補 `localStorage.clear()`；
(2) backend 健康狀態 dot 測試原用全域 `querySelector('span[aria-hidden]')`，但頁面另有 theme
按鈕圖示同款屬性，測試通過純屬僥倖命中 DOM 順序，改用 `getByText(...)` 先定位文字所在 row 再
scope 查詢。1 nice-to-have（`NavIcon` active/inactive 顏色分支未斷言）未採納，Approve。
backend 未動 1103 passed 不變；frontend 1148→1171 passed（55→56 files）、tsc 0、build OK。
`components/ui/*.tsx` 9 支 primitive 檔案測試評估至此全數完成。⚠ 附帶提醒：canonical routine
文件 `docs/routines/autonomous-daily-worker-prompt.md` 內文仍停在 v3（baseline
backend 638 / frontend 59，無「自我測試」/mutation 驗證/降級模式框架），已明顯落後於本次 cron
trigger 實際送入的 prompt（v4.1，baseline 1076/970），建議劉老師找時間把 cron trigger 目前
設定同步回這份文件。**2026-09-23（WMOM-20260923-06，A2 Part 4：跨情境差異圖）**：DEC-20260720-02
A2「情境比較分析」epic 完整範圍至此全數完成——差異圖判定不需要後端（比照 Part 3），純前端分桶
重採樣解決多情境序列取樣點不對齊的問題：`utils/scenarioTimeline.ts` 新增 `medianInterval`/
`pickBinMs`/`binSeries`/`buildDiffSeries` 純函式，`utils/scenarioHistoryFetch.ts` 抽出 Part 3
既有 fetch 邏輯供新頁籤重用，新增 `ScenarioCompareDiffView.tsx`（baseline 可選、`compare −
baseline` 疊圖）掛上 `ScenarioCompareAcrossView` 第三個頁籤。開發中自行抓到並修正 2 個真實 bug
（`hasAnyDiff` 誤把「聯集後桶陣列非空」當成有資料；元件測試 `getByText` 與 `<option>` 撞名，
改 `data-testid`）。code-reviewer review：1 must-fix（`usedFallbackAlign` 抓了卻未提示使用者，
差異圖把兩條線相減成一個數字時，一真一假對齊會算出看似精確卻誤導的差異，已補 banner）+
3 should-fix 全數採納（`truncated` 同款補提示；抽出的 `fetchScenarioHistory` 在 HTTP 失敗分支
與原內嵌寫法在「失敗+缺 sim_start」組合上行為分歧，已修正一致並補測試；`useMemo` 省略依賴補
說明註解）+ 3 nice-to-have 採納（decision_log 回填已解決、`binSeries` 補 3+ 點/桶測試、baseline
失敗時排除通用提示避免語意重疊），Approve。backend 未動 1103 passed 不變；frontend
1171→1216 passed（56→58 files，+45 新測）、tsc 0、build OK。**2026-09-23（WMOM-20260716-06）**：
footprint CPU-torch pin follow-up——多個 session 因「本地無 docker daemon」擱置，本次 preflight
發現本 sandbox 這次可手動啟動 `dockerd` 成功，接手實測收尾。`Dockerfile` 新增 1 行 CPU-only
torch wheel pin（`pip install torch --index-url .../whl/cpu`，在 `pip install -r
requirements.txt` 之前），真實 `docker build`/`docker run` 驗證：image content size
3.37GB→550MB（省 ~2.8GB，優於 DEC-20260716-02 原估「砍半」），`torch.cuda.is_available()`
True→False，`sentence_transformers`/`chromadb` import smoke test 正常，疊上真正 app 程式碼層
起容器 `/api/health` 回應 200 OK 正常開機。code-reviewer review：0 must-fix，2 項
should/nice-to-have（ISSUES.md 統計表同步、狀態標記格式）已採納。附帶重新調查
WMOM-20260509-F6（PostgreSQL row-lock test）範圍，判定遠比原估「0.5 工作天」大（repo 完全
無 Postgres 連線路徑，需先有架構決策才能動工），更正估時並標記 🟡 需劉老師決策，本次不接、
避免未來 session 誤判重工。backend/frontend 測試數量不受影響（Dockerfile-only 變更，CI 本來
就不 build Docker image）。**2026-09-23（WMOM-20260923-07）**：`WMOM-20260507-02`（PageHeader
placeholder 按鈕清單）sub-task a——`FarmOverview.tsx`「匯出」鈕接線：`handleExportSnapshot` 走
既有 `authFetch` 打 `GET /api/export/snapshot`（後端 `require_authenticated()` 閘門）→
`resp.blob()`（不解析、保留原始位元組）→ 沿用 reporting module 既有 `downloadBlob` 工具觸發下載
`farm-snapshot-{YYYY-MM-DD}.json`，失敗僅 `console.error`（沿用劉老師既有決定不彈 alert）。新增
4 測（happy path / authFetch header / loading disabled / 失敗態），皆 mutation-verified。
code-reviewer review：0 must-fix，2 should-fix 全數採納（happy-path 原測試用
`expect.any(Object)` 無法區分 `authFetch` 與裸 `fetch`，補專測直接斷言 `Authorization` header
並 mutation-verified 抓到回退裸 fetch 的 regression；測試檔一則誤導性註解已更正）+ 3
nice-to-have（同檔案 farm-trend fetch 同款缺口登記為新 follow-up **WMOM-20260923-08**、不阻塞；
unmount guard／UTC 檔名日期判定非阻塞未採納），Approve。backend 未動 1103 passed 不變；frontend
1216→1220 passed（+4 新測）、tsc 0、build OK。`WMOM-20260507-02` 清單尚餘 5 項（b~f）。
**2026-09-23（WMOM-20260923-09）**：`WMOM-20260507-02` sub-task b——`TurbineDetail.tsx` PageHeader
「停機」placeholder 鈕接上 `POST /api/control/command { command: 'stop' }`（走 `authFetch`）。
認領時追查發現右側「操作控制」卡片（`OperatorControlCard`）本身 4 處既有 `fetch` 呼叫（GET
status 輪詢 + POST command + POST curtail ×2）全部裸 `fetch`、未帶 `Authorization` header，且
`command`/`curtail` 兩端點皆 `require_role(SUPERVISOR)`——比先前 WMOM-20260923-08 的
farm-trend 缺口風險更高（enforce 開啟後操作控制面板整面失效）；同一次一併修復。新增 7 測皆
mutation-verified（含既有測試從未驗證 headers 而未鎖住 `clearCurtail` 這一缺口，本次補齊）。
code-reviewer review：0 must-fix，1 should-fix（`ISSUES.md` 父 issue checklist 未同步勾選）已
採納，1 nice-to-have（既有 `resp.ok` 缺錯誤處理，非本次引入）未採納維持現狀，Approve。backend
未動 1103 passed 不變；frontend 1220→1227 passed（+7 新測）、tsc 0、build OK。⚠ 稽核過程發現
**另有 7 支元件同款 authFetch 缺口**（`CostPage`/`EventComparisonView`/`FarmSelector`/
`FaultInjectionPanel`/`HistoryPage`/`SettingsPage`/`TrendChartPanel`，其中 `FarmSelector`/
`FaultInjectionPanel`/`SettingsPage` 含 SUPERVISOR/ADMIN 寫入），登記為新 follow-up
**WMOM-20260923-10**，標記為 M6 auth cutover（`WMOM-20260716-05i`）前置阻塞項，未在本次修復。
next_milestone: "【2026-07-18 — auth 全面完成，14 PR 進 main #109-#122】auth 基礎層 done (#108) → DB-backed user store + admin API (#110) → 雙模式授權 scaffold (#112) → 全 5 module router 授權遷移 (#113/#115-#117/#120-#122，含 workflow 4 router + cost + reporting + knowledge + monitoring 9 router 共 61+ 端點) → 前端 auth client + token 帶入 (#118) → 前端登入頁 + AuthProvider (#119) → GuidedTourPage (#114)。backend 998 tests collected / CI 涵蓋全 6 module + monitoring/physics + e2e。**2026-07-18 追加**：實測機組資料 → 修 Settings 改風速無反應（WMOM-20260718-01, PR #123）+ 拍板**模擬雙軌 DEC-20260718-01**（Scenario 批次生成為主 / Live 實接連續落地；WMOM-20260718-02~05）。**scenario-setup（WMOM-20260718-03）**：後端 generate_bulk 排程故障 primitive（#125 merged）+ 前端「情境」獨立頁 ScenarioPage（本 PR）。**下一步**：狀態可見性/顯示重設計（-04/-05）；並行 WMOM-20260716-06（footprint CPU-torch pin）+ M5 收尾 + M6 客戶接觸（WMOM-20260503-05）。 **2026-07-20 情境模式深化 arc**：(a) generate-bulk 並發 writer 撞 SQLite `database is locked` 根治——decouple `_bulk_running` + `pause_live_for_batch` context + 單交易 `store_readings`（WMOM-20260720-01, #142 merged）；(b) 定 **DEC-20260720-01「情境＝凍結資料集」**：設定頁依實際來源 gate（view/live/scenario 時停用模擬參數，#146）+ 選「產生情境」不再自由跑連續產資料（`run_loop` 貫穿、新 `source_kind='scenario'`、不起 Modbus，#147）；(c) 立 **DEC-20260720-02 情境比較分析 epic**（序列 B→A0→C 與 A1/A2 並進）：**A0 情境摘要端點** `GET /api/scenarios/{id}/summary`（每台機組物理聚合〔容量因數/累積損傷/結束 RUL/極限負載/DEL/故障數〕+ 風場 rollup，storage 層 SQL 聚合，#148 merged）、**A1 同情境內比較視圖**（消費 A0、faulted vs healthy 分群比較圖表，#150 merged；round-2 review 的 4 個
Should-fix follow-up 見 WMOM-20260720-13）。**WMOM-20260720-04 + -08 live/OPC 後端硬化（2026-09-22
merged）**：M6 現場部署唯一硬阻塞已清除——DataBroker.stop() 補停 OPC adapter、切走 live 對稱要求
SUPERVISOR、set_simulation 對非即時模擬來源改 409 拒絕、start/stop/switch_mode 加 RLock、engine._loop
睡眠改可中斷 Event.wait。**WMOM-20260922-01（session #2）**：補 accelerated 分支同款
stop() 響應性殘留問題，monitoring 1094 passed。**WMOM-20260720-13（session #3，PR #157 merged）**：
A1 round-2 的 4 個 Should-fix 全修。**WMOM-20260922-02（session #4，PR D）**：`GuidedTourPage`
inline component remount 修（DEC-20260720-01），frontend 961 passed。**WMOM-20260922-03（session
#5）**：A2 Part 1 跨情境摘要並排端點 `GET /api/scenarios/compare`，backend 1103 passed（詳見上方
last_updated）。**WMOM-20260922-04（session #7）**：A2 Part 2 前端——消費 A2 Part 1 端點的
`ScenarioCompareAcrossView`（跨情境風場層 rollup 摘要並排：headline + 指標選擇 + 長條圖 + 全指標
並排表），frontend 961→978 passed（+17 新測，含開發中自行抓到並修正的 2 個真實 bug：並排表格式化
誤用目前選中指標的格式化函式、刪除已勾選情境未同步清除勾選）。**WMOM-20260923-03**：A2 Part 3——
跨情境相對時間對齊時序疊圖，判定不需後端端點（純前端用既有 history 端點 + `sim_start` 算相對
時間，決策記錄 `DEC-20260923-01`），frontend 1067→1100 passed（+33 新測，詳見上方 last_updated）。
**WMOM-20260923-04**：`components/ui` primitives（`StatusPill`/`Charts`）測試補齊，9 支零測試
UI primitive 逐檔評估後選 2 支值得補（其餘 7 支 ROI 低或範圍另計），frontend 1100→1148 passed
（+48 新測，詳見上方 last_updated）。**WMOM-20260923-05**：`Sidebar.tsx` component render 測試
（+23 新測，詳見上方 last_updated），`components/ui/*.tsx` 9 支 primitive 檔案測試評估至此
全數完成。**WMOM-20260923-06（A2 Part 4：跨情境差異圖，詳見上方 last_updated）**：判定不需後端，
純前端分桶重採樣解決多序列取樣點不對齊問題，DEC-20260720-02 A2 epic 完整範圍（摘要並排＋疊圖＋
差異圖）至此全數完成。frontend 1171→1216 passed（+45 新測）。**WMOM-20260923-07**：
`WMOM-20260507-02` sub-task a——`FarmOverview.tsx`「匯出」鈕接線 `GET /api/export/snapshot`，
frontend 1216→1220 passed（+4 新測，詳見上方 last_updated）；附帶登記 follow-up
**WMOM-20260923-08**（同檔案 farm-trend fetch 補 `authFetch`，low priority 技術債，🔵
autonomous-friendly，15 min 可完工）。**WMOM-20260923-09**：`WMOM-20260507-02` sub-task
b——`TurbineDetail.tsx` 停機鈕接線 + `OperatorControlCard` 4 處既有 `fetch` 補 `authFetch`
（該卡片指令端點皆 `SUPERVISOR`-only 寫入，風險高於 farm-trend），frontend 1220→1227 passed
（+7 新測，詳見上方 last_updated）；附帶稽核發現 **WMOM-20260923-10**（另 7 支元件同款
authFetch 缺口，標記 M6 auth cutover 前置阻塞，high priority）。
**WMOM-20260923-08（同日追加）**：`FarmOverview.tsx` `TrendCard` farm-trend fetch 補
`authFetch`（WMOM-20260923-07 review 登記的 nice-to-have follow-up，`FarmOverview.tsx`
全檔至此無裸 fetch 殘留），frontend 1252→1254 passed（+2 新測）。review 中遞迴重掃
`frontend/components/` 全樹發現 `components/field/MyOrdersMode.tsx:31` 仍是裸
fetch（`WMOM-20260923-10` 原稽核未遞迴子目錄漏掉），新開 follow-up
**WMOM-20260924-08**，估時 15 min，🔵 autonomous-friendly，下個 session 可優先撿。
**下次接手**：`WMOM-20260507-02`（PageHeader placeholder 按鈕清單）**a/b/c/e/f 全數完成**
（見上方 last_updated，最新 WMOM-20260925-04 為 f），**清單至此僅剩 sub-task d**（風機細節
『安排檢查』，依賴 `WMOM-20260505-22` `inspection_schedule` 尚未做，持續卡著，非單 session
可清楚定義範圍除非同時動 -22）。次要可選：PR C（檢視情境掛載 app，需先寫 broker 子設計）；
WMOM-20260716-06（footprint CPU-torch pin）**已於 2026-09-23
session 完成**；WMOM-20260509-F6（PostgreSQL row-lock）2026-09-23 重新調查後判定需先由劉老師
拍板是否選 PostgreSQL backend，🟡 不再列 autonomous 候選；M5 收尾 + M6 部署前置（HTTPS 配置，
需先定部署目標/憑證策略，非單純 autonomous 可決）。auth cutover 檢查表尚餘 3 項需劉老師/現場
配合（4 支 workflow router 遷移狀態確認、admin bootstrap 真實使用者佈建、staging enforce=true
完整 lifecycle demo），🟡 非純 autonomous 可決，建議下個 session 檢視是否已具備條件推進。
**CI runner 基礎設施已連續多個 session 觀察到綠燈**（見上方 last_updated 的 session #6/#7 記錄），
若下個 session 的 PR 也順利 auto-merge，建議清除本檔舊警語。附帶提醒：canonical routine 文件
`docs/routines/autonomous-daily-worker-prompt.md` 內文仍停在 v3（baseline backend 638 /
frontend 59），已明顯落後於本次 cron trigger 實際送入的 prompt（v4.1，baseline 1103/1267），
建議劉老師找時間把 cron trigger 目前設定同步回這份文件（WMOM-20260923-05 work-log 已提醒過，
本次二次提醒）。
**2026-09-25（WMOM-20260505-22 後端完成，autonomous session）**：`inspection_schedule` 定檢
計畫 + scheduler auto-spawn 後端全套（domain/repository/service/router，79 測皆
mutation-verified；code-reviewer 抓到 1 must-fix + 1 should-fix 皆已修復，詳見上方
last_updated 與 ISSUES.md WMOM-20260505-22 completion summary）。backend 1103→1182
passed，frontend 未動 1288 passed 不變。**`WMOM-20260507-02` sub-task d 的依賴已解除，
但前端本身未做**——拆成新 **WMOM-20260925-05**（open，API 已齊全，下個 session 可直接
接手：`TurbineDetail.tsx` 安排檢查鈕 + `/admin/workflow/inspection` 定檢計畫管理頁）。
**下次接手**：優先 WMOM-20260925-05（前端，無設計歧義）；其餘見 TODO.md「需劉老師決策」
清單與 PR C（需先寫 broker 子設計）。"
technologies:
  - FastAPI
  - React
  - SQLite
  - Modbus TCP
  - OPC UA (Bachmann Z72)
  - Docker
outputs:
  - tool
  - paper
  - presentation
description: "Operations & Maintenance platform for offshore wind farm service providers. Five modules — monitoring, workflow, cost, reporting, knowledge — with simulator-first demo path."
description_zh: "離岸風場運維廠商工具：監控 + 庫存派工 + 成本計算 + 報表 + 警報手冊查詢。從 digiWindTurbine（物理模擬器 + SCADA 平台）商業化升級而來，目標 2026-Q4 第一個運維廠商客戶 PoC + 第一筆合約（NT$2-4M）。"
key_metrics: "5 modules / 6-month roadmap / Z72 first-customer reference"

# ──────────────────────────────────────────────
# windMindOM 額外欄位（v0.8.1 baseline）
# ──────────────────────────────────────────────
product_version: "v0.8.1"
roadmap: "docs/product/ROADMAP.md"
vision: "docs/product/PRODUCT_VISION.md"
architecture: "docs/product/MVP_ARCHITECTURE.md"
decision_log: "docs/product/decision_log.md"

milestones:
  M1:
    name: "Setup baseline"
    target_month: "2026-05"
    status: in_progress
    progress: 85   # WMOM-20260503-01/02/03/04 done; -05 infrastructure done，contact 持續中
  M2:
    name: "Cost module（從 ECN 移植）"
    target_month: "2026-06"
    status: completed
    progress: 100  # -01..-08 全 done (skeleton + 4 engine + adapter + schemas + API + frontend) — 1 天完成
  M3:
    name: "Workflow Part 1（Work Order + Approval）"
    target_month: "2026-07"
    status: completed
    progress: 100  # backend 5（-14/-15/-16/-17/-18） + frontend 2（-19/-20）全 done；衍生 -21/-22 不阻塞 M3 主線
  M4:
    name: "Workflow Part 2（Inventory）+ Reporting"
    target_month: "2026-08"
    status: completed
    progress: 100  # A1-A10 全 done + 4 frontend（reports / approval / material / inventory）全收；F1-F6 follow-up + WMOM-20260518-01 + WMOM-20260509-F1 為 nice-to-have，不影響 M4 完成度
  M5:
    name: "Knowledge / RAG + 現場 mobile UI"
    target_month: "2026-09"
    status: in_progress
    progress: 75  # +M5-3/4 完整 Z72 手冊向量檔 done (WMOM-20260608-03，531 chunks)；M5-1~6 + Part A/B-1/B-2 + 真語料皆就位；剩客戶手冊擴充 + 一年警報 csv 灌入（M6 部署期）
  M6:
    name: "第一個運維廠商 PoC + 第一筆合約"
    target_month: "2026-10"
    status: in_progress
    progress: 25  # auth 後端全面完成 + live/OPC 後端硬化收尾（唯一硬阻塞已清除，WMOM-20260720-04/-08）；前端 authFetch cutover 前置作業已完成（WMOM-20260925-01，hooks/ 層缺口已修，cutover 檢查表已重新勾選）；剩 footprint / HTTPS 部署 / PostgreSQL row-lock / 客戶接觸；本次（WMOM-20260925-05）完成的定檢計畫 UI 屬 M5/UX polish，非 M6 critical path，不影響此百分比

issue_stats:
  open: 8  # 9→8（WMOM-20260926-03 完成，PR C Phase 1）
  in_progress: 0  # 不變
  blocked: 0
  done: 136  # 135→136（WMOM-20260926-03 完成）
             # total 144（8+0+136=144）
             # ⚠ 附帶發現（2026-09-26）：raw grep `### WMOM-*` + `**Status**` 欄位在
             # ISSUES.md 主文實測只有 114 筆（9 open+3 in_progress+102 done），與這裡/
             # ISSUES.md 頂部統計表的 141/142 對不上——推測歷史上有 issue 只存在於
             # `docs/legacy/issues_changelog_archive.md`（WMOM-20260529-02 抽 changelog
             # 時連帶影響），本次未展開全面稽核（超出 WMOM-20260505-21 範圍），沿用既有
             # delta-only 更新慣例，僅記錄供劉老師或未來 session 決定是否要開一個
             # dedicated 清點 issue。

inherited_baseline:
  source: digiWindTurbine
  status_snapshot: "docs/legacy/digiwt_STATUS.yaml"
  todo_snapshot: "docs/legacy/digiwt_TODO.md"
  highlights:
    - "104 SCADA tags（IEC 61400-12-1/2 anemometer/vane transfer functions）"
    - "11 fault scenarios（unified physics-coupled path）"
    - "Bastankhah-Porté-Agel Gaussian wake + dynamic meandering"
    - "Atmospheric stability × shear/TI/wake/veer/L_u 五重耦合"
    - "Fatigue + DEL + Miner damage + RUL"
    - "FastAPI + React + SQLite + Modbus TCP + Docker Compose"
    - "Data quality validation：18/21 checks passed (2026-04-12)"
